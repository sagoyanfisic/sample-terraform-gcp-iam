terraform {
  # Remote backend using GCS. Uncomment and fill in values before running
  # terraform init for the first time in a shared/CI environment.
  #
  # backend "gcs" {
  #   bucket = "my-terraform-state-bucket"
  #   prefix = "terraform-gcp-iam/develop"
  # }
}
